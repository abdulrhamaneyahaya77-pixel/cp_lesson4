{
  "name": "mini-erable",
  "version": "1.0.0",
  "description": "Mini calculateur de dérivée symbolique (TDD + Parcel + Jest)",
  "scripts": {
    "start": "parcel index.html",
    "dev": "parcel index.html --port 1234 --open",
    "test": "jest",
    "coverage": "jest --collect-coverage"
  },
  "devDependencies": {
    "@babel/core": "^7.19.1",
    "@babel/preset-env": "^7.19.1",
    "@parcel/babel-preset-env": "^2.16.0",
    "babel-jest": "^29.0.3",
    "jest": "^29.0.3",
    "parcel": "^2.7.0"
  }
}
